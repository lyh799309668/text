省创为什么立项
	自己想做省创，找了老师给了灵感，所以。。。
遇到什么困难
	对算法不熟悉，翻阅大量资料进行学习，对技术前沿进行调研，最终将算法实现
	用的vb，数据库access
	roc  100道题，每10题的第一题算一组。。。得到有10个点的roc曲线（线性回归），算出auc，记为结果
	
	作为负责人，没有经验，在工作中学习项目管理，对工作任务进行分解和分工，并对组员的完成情况进行检查并督促
毕设 t检验自己做的，使用的matlab

	列表是动态数组，它们不可变且可以重设长度
	元组是静态数组，它们不可变，且其内部数据一旦创建便无法改变
#能够被类和对象调用;入参为cls,只能够访问类的变量,不能够访问对象的变量.
#能够被类和对象调用;但入参不能为self和cls,因此不能访问对象和类的属性
@staticmethod不需要表示自身对象的self和自身类的cls参数，就跟使用函数一样。
@classmethod也不需要self参数，但第一个参数需要是表示自身类的cls参数。
with 语句适用于对资源进行访问的场合，确保不管使用过程中是否发生异常都会执行必要的“清理”操作，释放资源

类的构造函数，用于初始化类成员等，只能用init ，不能换其他名字
init方法在类的一个对象被建立时，马上运行。
Django有自己的ORM
tornado一般不使用自己的数据库

Django是多线程没有异步回调,并发低
tornado是单线程异步回调,并发高

Django的插件相对更丰富

